server:
  port: 8070

spring:
  config:
    activate:
      on-profile: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:30336/map_db?useSSL=false&allowPublicKeyRetrieval=true
    username: leehy
    password: q1w2e3!